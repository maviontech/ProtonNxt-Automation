[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    smoke: critical build-stability checks that must pass before sanity
    sanity: module-level functional coverage run after smoke passes
